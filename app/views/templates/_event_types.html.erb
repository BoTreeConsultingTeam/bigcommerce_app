<% event_types = event.present? ? event.event_types : [] %>

<div class="form-group">
  <%= select_tag 'template[event_type_id]', options_from_collection_for_select(event_types, "id", "name", event_type), class: 'form-control select optional', include_blank: false %>
</div>
